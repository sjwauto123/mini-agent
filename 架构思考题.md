# 架构思考题

### **模块一：Context / Performance**

#### 1.大模型面对第一轮长窗口或多模态输入时，first token 会显著变慢。有什么快速/低成本/用户体验也不差的方案？从5-10秒稳定压缩到2秒。

回答：

首token慢是因为，首轮请求通常没有可复用的 Prefix KV Cache，，所有首token就会慢，后续因为有固定前缀，提高了响应速度

第一件事，减少首轮真正给模型的上下文，按需加载。不要把所有信息一次性塞给模型，而是根据问题动态选择真正需要的内容，包括筛选需要的工具信息而不是加载所有的工具，以及长期记忆的话也是按需获取，包括skill也是渐进式披露

第二件事，对context做稳定性划分：分为固定前缀内容，动态变化内容，保证固定前缀内容多轮对话不变可以复用前面的kv cache，避免重复计算。

第三件事，对于图片处理，不要每次让模型重新处理原图片，可以使用ocr模型异步处理图片，得到图片描述等信息后，在给大模型并缓存起来。

第四，首token不一定是最终回答，可以将中间思考过程先打印出来，或者显露agent处理的业务状态，比如 Agent 需要调用一个比较慢的工具时，不一定要等工具执行完成后才给用户反馈，而是可以先通过 SSE 返回阶段性状态，例如“正在检索知识库”

第五，第一轮对话时把能并行初始化的全部提前做，比如用户agent配置，工具，模型，记忆等配置信息，当请求的时候并行处理这些没有依赖关系的操作。

第六，将一些中间结果或配置缓存，比如问题重写，任务状态，问题的向量化，RAG检索的结果，工具描述，agent等一些配置信息。

第七，进行意图分析和动态路由，如果是简单回答不走复杂工作流，直接回答。



#### 2.一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？

**一个 Session 连续聊 200 轮以后，我不会简单截断历史，而是做分层 Context 管理。最近 10～20 轮保留原文，保证当前对话的连续性；更早的历史通过摘要压缩，重点提取用户已经确认的事实、技术决策、任务状态、重要约束和未解决问题；真正长期有价值的信息再单独写入 Memory。**

**同时采用层级摘要和按需检索，用户当前的问题只检索相关历史，而不是把 200 轮全部重新放进 Context。压缩时保留摘要对应的原始消息范围，避免信息丢失；对于 Tool 状态和当前任务状态单独保存，不能随着聊天历史一起简单删除。**

**压缩触发也不应该固定按多少轮，而应该根据 Context Token 使用量动态触发，比如超过预算阈值就开始压缩。最后通过事实一致性、任务连续性和指代理解来验证压缩质量，确保压缩以后用户说“继续刚才的方案”时，Agent 仍然知道刚才讨论的是什么。**



### **模块二：Memory**

####  1.和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？

**如果用户半个月后再次问以前的问题，我不会简单把整个历史聊天记录塞回 Context，而是先判断当前问题是否需要长期记忆。如果需要，就根据当前问题生成 Memory Query，然后在用户自己的 Memory 范围内做混合检索，包括向量检索和关键词检索，再结合时间、重要性、主题匹配等因素进行 Rerank，最终只召回少量高相关 Memory。**

**如果发现当前问题和过去某次问题高度相似，还可以直接召回之前的问题、答案和相关上下文，让 Agent 基于历史回答继续生成。对于可能已经发生变化的信息，还需要做版本和有效性管理，不能无条件相信旧 Memory。这样既能让 Agent “记得用户以前聊过什么”，又不会因为召回大量历史信息导致 Context 变长和回答被旧信息干扰。**



####  2.你理解的 Agent memory 经典框架是什么？它的发展趋势是什么，最头部的玩家在怎么做？

**我理解的 Agent Memory 经典框架，可以分成短期记忆、长期记忆和任务工作记忆。短期记忆主要维护当前 Session 和最近上下文，任务记忆维护当前 Agent 执行到哪一步，长期记忆则保存用户偏好、历史事实以及过去任务的经验。比较经典的 MemGPT/Letta 也是把当前 Core Memory 和外部 Recall、Archival Memory 分开，本质上是把有限的 Context 当成高速工作内存，需要什么再从外部 Memory 取。** 

**它的发展趋势，我认为有三个比较明显的变化。第一，从保存聊天记录变成提炼结构化 Memory，不仅记用户说过什么，还要记长期偏好、重要决策和任务经验；第二，从被动召回变成主动写入和更新，Memory 需要有去重、冲突处理、版本和遗忘机制；第三，Memory 和 Context Engineering 开始融合，最终不是单纯做向量检索，而是根据当前任务，把相关 Memory、Session、RAG 和 Tool 信息动态组装进 Context。**

**头部玩家现在也基本往这个方向走。OpenAI 公开的 Agents SDK 已经把 Session 和 Memory 分开，并开始从过去 Agent Run 中提炼经验；Google 的 Gemini 更偏向 Personal Context，把过去聊天以及用户授权的数据连接起来做个性化和主动 Agent；Anthropic 则强调 Context Engineering 和长任务中的外部 Memory，通过 Memory Tool 让 Agent 在 Context Reset 后继续工作。** 

**所以我认为未来 Agent Memory 的核心不是建一个向量数据库，而是建立一个完整的 Memory Lifecycle：什么值得记、怎么存、什么时候召回、怎么更新、什么时候遗忘，再由 Context Manager 根据当前任务动态把真正有价值的记忆放进 Context。**



### **模块三：Task** 

#### 1.对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？

长程任务中，我不会单纯依赖模型的 Context 让它一直记住目标，而是把任务目标、约束、计划和执行进度从普通对话中独立出来，形成 Task State存储起来。执行过程中采用 Checkpoint，完成一个阶段就持久化当前状态；同时采用 Plan-and-Execute 先建立整体计划，再根据实际执行结果动态 Re-plan。对于长任务，还可以周期性做 Progress Check，检查当前是否偏离 Goal。这样即使对话 Context 被压缩，Agent 仍然可以从外部 Task State 恢复任务目标和获取执行进度。关键任务还可以把 Goal 转成结构化的完成条件，用状态校验代替单纯 Prompt 约束来确认任务已经完成了。

方案：

Checkpoint：可靠但需要状态管理；Plan-and-Execute：目标稳定但计划可能过时；Re-plan：适应性强但增加模型调用；Reflection：能发现跑偏但增加成本；硬约束：可靠性最高但开发成本也最高。



#### 2.用户给 Agent 下达任务：每天早上 9 点根据昨天聊天情况做复盘总结。你会怎么设计？

**Agent 收到用户的问题和指令**，判断这是一个周期性任务，然后调用“创建定时任务工具”，把任务信息写入定时任务数据库，例如执行时间、执行周期、任务内容、用户 ID 等。

**Scheduler 定时调度器监听定时任务**，到了任务定义的执行时间，就创建一次具体的执行任务，也就是一次 Task Run，并交给后面的 Agent Worker 执行。

**Agent Worker 开始执行这个任务**，可以理解成 Agent 收到了一次系统自动产生的任务指令，例如“请根据昨天的聊天内容生成复盘”。然后 Agent 根据任务要求获取昨天的聊天记录。如果聊天内容比较少，可以直接作为上下文交给模型；如果内容过多，就根据上下文预算进行**检索、筛选、分段总结或者压缩**，提取与当前任务最相关的关键信息，再交给大模型。

**最后由 Agent 根据整理后的上下文生成复盘总结**，返回给用户；如果这个复盘结果中存在值得长期保存的信息，还可以进一步提取并写入 **Memory**。



### **模块四：Tool / Session Runtime**

#### 1.Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？

我会把异步 Tool 和同步 Tool 区分开。对于执行时间比较长、但结果又比较重要的工具，Agent 不应该一直阻塞等待，而是创建一个异步任务并生成唯一的 task_id，将任务持久化后发送到消息队列，然后立即告诉用户任务已经提交。后台 Worker 从队列获取任务并执行 Tool，同时记录任务状态和执行结果。任务完成后把结果持久化，再通过 WebSocket、SSE、站内消息或者其他通知渠道通知用户。如果执行失败，则根据任务是否支持重试进行重试，同时通过 task_id 做幂等控制，避免重复执行。这样可以实现**请求快速返回、任务可靠执行、结果不丢失、完成后主动通知**。



#### 2.如果 session state 为 busy，此时用户又发来新消息，或者异步工具完成事件也到达，runtime 应该如何处理？

如果 Session 处于 Busy 状态，我不会允许多个 Agent Run 同时修改同一个 Session，而是通过 Session 级事件队列进行串行化。用户的新消息默认进入队列，等待当前 Run 完成；如果是异步 Tool 完成事件，则通过 `session_id + run_id + tool_call_id` 找到原来的 Agent Run，将 Tool Result 注入对应上下文并恢复执行。如果用户消息和 Tool 完成事件同时到达，也统一进入事件队列，由 Runtime 根据当前 Run 状态和事件归属进行处理。同时需要通过 event_id、run_id 和幂等机制防止重复事件和旧事件污染当前 Session。对于支持打断的 Agent，还可以取消当前 Run，再处理用户的新消息。



### **模块五：Agent Runtime 架构对比**

#### 1.Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？

**Claude Code 和国内 GLM、豆包等 OpenAI-compatible Agent，在最基本的工具调用机制上其实没有本质区别，都是 LLM 决定下一步需要使用什么工具，Runtime 负责执行工具，然后把 Tool Result 反馈给 LLM，LLM 再根据结果决定继续调用哪个工具还是结束任务。**

**区别主要在工具协议和工具执行体系。OpenAI-compatible Function Calling 通常采用标准化的 tool_calls / tool result 形式，优点是简单、兼容性强，Runtime 比较容易统一管理权限、超时、重试和审计；缺点是复杂任务中可能产生大量模型往返，而且大量 Tool Result 会进入上下文。**

**Claude 的 Tool Use 在消息结构上采用 tool_use 和 tool_result content block，而不是完全照搬 OpenAI 的 tool role。同时 Claude 还提供了客户端工具、服务端工具以及程序化工具调用。程序化工具调用允许模型生成代码，在执行环境中连续调用多个工具，并在代码里完成循环、条件判断、过滤和聚合，最后只把处理后的结果交给模型，因此可以减少模型往返和上下文膨胀，但 Runtime 和安全体系也更加复杂。**



#### 2.Openhands的状态机设计有什么优缺？更优雅的实现方式是怎么样的？

**OpenHands 的状态机设计，本质上是用 State 表示 Agent 当前任务状态，通过 EventStream 传递 Action 和 Observation，再由 AgentController 驱动 Agent 一步一步执行。它的优点是执行流程清晰、Agent 和 Runtime 解耦，而且事件驱动天然适合异步工具、长任务以及状态恢复。**

**但如果把所有业务流程都设计成状态，随着 Agent 增加工具执行、用户打断、重试、权限审批、上下文压缩等能力，状态数量和状态转换关系会快速膨胀，Controller 也容易变成一个复杂的状态机。**

**我更倾向于采用“有限状态机 + 事件驱动 + Task/Executor”的设计。State 只负责表示 Session 生命周期，例如 IDLE、RUNNING、PAUSED、FINISHED、FAILED；具体的 LLM 调用、Tool 执行、用户消息、异步任务完成都抽象成 Event，通过 Event Queue 驱动 Runtime。这样可以把“当前是什么状态”“发生了什么事情”和“怎么执行”三个问题分离，既保留状态机的可控性，又避免状态爆炸。**
