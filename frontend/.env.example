# Copy this file to frontend/.env only if the backend is NOT on the default
# http://127.0.0.1:8000. The Vite dev server reads its env from this directory,
# so a VITE_* variable placed in the repository root .env has no effect here.
VITE_API_TARGET=http://127.0.0.1:8000
