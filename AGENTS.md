# AGENTS.md — 在这个仓库里用 AI 改代码的规则

这份文件不是"使用建议"，是**约束**。它来自本项目三天 60 个提交里真实发生过的事故，
每条规则后面都写着它是为了拦住哪一次。

---

## 0. 唯一一条原则

> **AI 跑通 ≠ 验收通过。**

本项目最典型的证据：2026-09-15 上午，`tools/qa/qa_behavior.py` 跑出 **127/127 全绿**；
两小时后，用户贴出一段界面的思考过程问"这对吗"——立刻暴露三个 bug：

| 漏掉的 bug | 为什么自动检查没抓到 |
|---|---|
| 思考面板复述协议提示（与提示词逐字重合 41 字） | 检查只覆盖 `answer` 通道，**从不检查 `thinking` 通道** |
| 执行日志字数恒显示"0 字" | 契约测试冻结了**事件类型**，没冻结**字段名**（后端已改名） |
| 运行期间切到执行日志不再刷新 | 闭包捕获旧 `view`，属于交互时序问题，静态检查不可见 |

**结论**：AI 写的测试覆盖的是"它想到的"，不是"应该覆盖的"。
所以下面三类动作，人的眼睛不可替代。

---

## 1. 三类动作必须人工过目

### 1.1 页面 / 流式 / 展示层改动

**范围**：`frontend/src/**`、`backend/mini_agent/runtime.py`、`events.py`、`routers/sse.py`

**为什么**：这三处的问题几乎全部只在真机渲染时可见，且历史上每次都是用户先发现的。

**过目清单**（打开 `http://127.0.0.1:8000` 实际看一眼，不要只看代码或测试报告）：

- [ ] 字数备注不是"0 字"；旧会话的字数仍读得出来
- [ ] 思考面板没有复述协议提示词
- [ ] 回答气泡不重复；发新消息时**上一条回答没有被清空**
- [ ] 首片回答早于模型结束到达（是真流式，不是整段蹦出）
- [ ] 上游重试会**撤回**半句而不是叠成"半句 + 完整句"
- [ ] 运行途中切到执行日志，步骤数会**继续增长**，不是等结束才出现
- [ ] 失败运行如实显示错误条；成功运行不显示错误条

### 1.2 跨文件删除

**为什么**：把 `storage.py` 改成包时用了 `git rm -f`，在提权 shell 里连带删掉整个
`backend/`（17 个文件），**当时未提交的改动全部回退到 HEAD，必须重做**。

**规则**：

1. **动手前先提交**。高风险操作前工作区必须是干净的——否则一次误删就把几小时工作清零。
2. 模块改包用**普通 `rm`**，永远不要用 `git rm -f`。
3. 每次删除**逐条确认路径**，不写通配符。
4. 恢复方式记住：`git restore --source=HEAD --staged --worktree <路径>`。

### 1.3 提交前

**规则**：

1. 跑一遍验收并**看证据**，不要只看 AI 的总结：
   ```bash
   python tools/qa/run.py all
   ```
   证据落在 `tools/qa/evidence/*.json`，其中记录了**脚本哈希 + 提交号 + 服务与模型状态**。
   没有这三样，"通过了"无法定位到任何可复现的状态。
2. 证据要比改动**新**（门禁 4 会提醒）。
3. 确认 `git rev-parse HEAD` 与 `origin/main` 的关系符合预期——**这个仓库可能被并行推进**
   （曾出现半小时内 HEAD 前进两个提交的情况），别在过期的 HEAD 上做判断。

---

## 2. 零风险任务的机械门禁

「只加注释」「只是折行」「只是拆分模块」这三类任务在 diff 上看着无害，
但它们是本项目的**事故高发区**：

| 事故 | 后果 |
|---|---|
| 补注释时整文件重写 `runtime.py` | 丢掉尾部 303–358 行 |
| 补注释时重写 `SYSTEM_PROMPT` | 多出 2 个前导空格，4 行被改 |
| 折行脚本括号配对写下标 | `unterminated string literal` |
| 拆分模块时 `git rm -f` | 见 §1.2 |

根因是**同一件事**：任务要求"外科手术"，AI 的默认行为是"整文件重写"。

### 强制规则

1. **先提交**，再做这类改动（工作区干净才动手）。
2. **AST 等价校验**，不看 diff 凭感觉：
   ```bash
   python tools/gates/check_ast_equiv.py --staged
   ```
3. `注释：` 类提交的 **deletions 必须为 0**（只增行、不删行）。
4. **禁止** `git rm -f`；**禁止**用整文件 `Write` 去改已有文件（用锚定插入或 `Edit`）。
5. **一次只做一类改动**，不要把折行和逻辑修正混在同一个提交里。

### 谁在拦你

`.githooks/` 下的两个钩子会把这些规则变成真正的阻断（启用方式见 `.githooks/README.md`）：

- `pre-commit` — 批量删除、人工过目、证据过期提醒
- `commit-msg` — 结构等价、注释类只增不删

如果你的任务确实要改变行为，就**用对的前缀**（`修复：`/`功能：`/`重构：`），
门禁自然放行——规则不想拦你，只想阻止你把行为变更伪装成零风险变更。

> **为什么结构等价校验在 `commit-msg` 而不是 `pre-commit`**：
> `git commit` 先跑 pre-commit，**之后**才写提交信息。所以 pre-commit 阶段读
> `.git/COMMIT_EDITMSG` 拿到的是**上一条**提交的信息，会让门禁"看情况随机生效"。
> 这个 bug 单元测试全绿也没抓到，是真跑 `git commit` 才暴露的
> （现已固化为 `tools/gates/test_hooks_e2e.py` 的回归用例）。
>
> 一般化的教训：**门禁依赖什么输入，就必须放在能拿到那个输入的阶段**。

---

## 3. 验证资产在哪

| 位置 | 是什么 | 命令 |
|---|---|---|
| `tools/qa/` | 可复跑的验收套件，产出结构化证据 | `python tools/qa/run.py list` / `all` / `smoke` |
| `tools/qa/evidence/` | 每次运行的结果（含脚本哈希、提交号、逐项断言） | 直接读 JSON |
| `tools/gates/` | 结构等价校验 + 门禁自身的测试 | `python tools/gates/selftest.py` |
| `.githooks/` | 提交前门禁 | `git config core.hooksPath .githooks` |
| `tests/` | pytest 单元/集成/浏览器用例 | `python -m pytest` |
| `docs/` | 需求、设计、决策记录、验收报告、AI 协作记录 | — |

**这批脚本此前散落在 `.pytest-tmp/`（被 gitignore），后果是**：
评审拿不到证据、CI 拿不到、换个时间自己复跑不出同样结论。
具体表现：`trace_fields_browser.py` 里曾**写死 4 个会话 UUID**，
会话一被清理就变成 6 项**假失败**——因为脚本从没在第二个环境跑过，所以没人发现。

**写验收脚本时的三条约定**（详见 `tools/qa/README.md`）：

1. 前置条件机器可检，不满足就报 `BLOCKED` 并说清原因；
2. **缺 fixture 报 `SKIP`，不报 `FAIL`**；
3. **不要硬编码会话 id / 端口 / 历史数据快照**——按能力自发现。

---

## 4. 已知环境陷阱

这台机器有若干"看起来能用、实际上会咬人"的地方。踩过的都记在这里，省下重复排查：

| 现象 | 原因 / 对策 |
|---|---|
| `sort -rn`、`timeout` 报奇怪的语法错 | 解析到了 **Windows 版本**的 `sort.exe` / `timeout.exe`。脚本里改用 `awk`；给命令设时限用别的方式 |
| Python 访问 `127.0.0.1` 得到 502 | 系统代理。`urllib` 用 `ProxyHandler({})`，`curl` 加 `--noproxy '*'` |
| `npm` / `npx` 报"程序被安全策略阻止" | `npm` 是 bash 脚本，被执行时路由到 WSL，而 `wsl.exe` 在安全软件黑名单里。改用 `node node_modules/npm/bin/npm-cli.js` |
| 服务起不来：`data directory is already in use` | 单实例锁 `.mini-agent/instance.lock`。同一数据目录只能一个进程；换目录用独立实例做验证 |
| 改了后端没生效 | 必须**重启持锁进程**；改前端要 `npm run build` 后静态目录才更新 |
| 浏览器套件报 playwright 缺失 | 只有项目 `.venv` 装了 playwright，用 `.venv/Scripts/python.exe` 跑 |
| `rm -rf <目录>` 被拦、目录连 `ls` 都拒绝访问 | 两层原因：① agent 侧 safe-delete 护栏（逐文件 `rm -f` 不受影响）；② 目录自身 ACL deny（非管理员无解，只能用户用管理员 `takeown` + `icacls /reset` 处理） |
| `tasklist` / PowerShell 的 stdout 不回显 | 输出写文件再读；`taskkill` 要写 `MSYS_NO_PATHCONV=1 taskkill /PID <pid> /F` |
| 全量 pytest 出现大量 `SystemExit` fixture 错误 | 刚 kill 掉服务后锁/端口未收敛的瞬态，重跑即恢复。跑之前先确认 8000 没残留 |
| 行尾不一致 | `frontend/src/App.tsx`、`styles.css` 是 **CRLF**，其余 60 个文件是 **LF**，`core.autocrlf=false`。工具改写行尾会被逐行计成"删除+新增"，可能触发门禁 2b |

---

## 5. 提交约定

- **中文前缀**，一模块一提交：
  `修复：` / `功能：` / `重构：` / `前端：` / `后端：` / `测试：` / `文档：` / `工程：` / `注释：` / `格式：`
- `注释：` / `格式：` / `工程：` **会被门禁 2 做结构等价校验**——只有真的零行为变更才用它们。
- 提交信息写清楚**改了什么、为什么**，不必写"如何做"（diff 里有）。
- 不要把行为变更和格式化混在一个提交里。

---

## 6. 关于记忆与记录

`.workbuddy/memory/` 下是工作日志与项目长期笔记（被 gitignore，不随仓库交付）。
写日志时克制一点：**只留决策、踩坑、结论**，不要逐次调试的流水账
（曾出现单日 595 行 / 78KB 的记录，维护成本已经超过它的价值）。
